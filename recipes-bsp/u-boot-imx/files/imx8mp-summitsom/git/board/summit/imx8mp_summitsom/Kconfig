if TARGET_IMX8MP_SUMMITSOM

config SYS_BOARD
	default "imx8mp_summitsom"

config SYS_VENDOR
	default "summit"

config SYS_CONFIG_NAME
	default "imx8mp_summitsom"

config SUMMIT_SECURE
	bool "Secure image" 

source "board/freescale/common/Kconfig"

endif
