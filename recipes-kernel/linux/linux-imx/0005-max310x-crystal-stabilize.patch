--- a/drivers/tty/serial/max310x.c	2021-03-02 08:48:20.000000000 
+++ b/drivers/tty/serial/max310x.c	2021-03-02 01:04:46.000000000 
@@ -611,7 +611,7 @@
 	/* Wait for crystal */
 	if (xtal) {
 		unsigned int val;
-		msleep(10);
+		msleep(100);
 		regmap_read(s->regmap, MAX310X_STS_IRQSTS_REG, &val);
 		if (!(val & MAX310X_STS_CLKREADY_BIT)) {
 			dev_warn(dev, "clock is not stable yet\n");
