
Upstream-Status: Inappropriate No time for upstreaming  

diff --git a/drivers/gpu/drm/imx/dw_mipi_dsi-imx.c b/drivers/gpu/drm/imx/dw_mipi_dsi-imx.c
index 0fca33f54f6a..be9c584ccafd 100644
--- a/drivers/gpu/drm/imx/dw_mipi_dsi-imx.c
+++ b/drivers/gpu/drm/imx/dw_mipi_dsi-imx.c
@@ -561,7 +561,8 @@ dw_mipi_dsi_imx_bind(struct device *dev, struct device *master, void *data)
 
 	ret = drm_bridge_attach(&dsi->encoder, bridge, NULL, 0);
 	if (ret) {
-		DRM_DEV_ERROR(dev, "failed to attach bridge: %d\n", ret);
+		if (ret != -EPROBE_DEFER)
+			DRM_DEV_ERROR(dev, "failed to attach bridge: %d\n", ret);
 		return ret;
 	}
 
@@ -635,7 +636,8 @@ static int dw_mipi_dsi_imx_probe(struct platform_device *pdev)
 
 	ret = component_add(dev, &dw_mipi_dsi_imx_ops);
 	if (ret) {
-		DRM_DEV_ERROR(dev, "failed to register component: %d\n", ret);
+		if (ret != -EPROBE_DEFER)
+			DRM_DEV_ERROR(dev, "failed to register component: %d\n", ret);
 		dw_mipi_dsi_remove(dsi->dmd);
 	}
 
